<% @posts.each do |post| %>

  <h2><%= post.title %></h2>

  <span><%= post.created_at.strftime('%b %d %Y') %></span>

  <p><%= post.body %></p>

  <span><%= link_to 'Изменить', edit_post_path(post) %></span>
  <span><%= link_to 'Удалить', post_path(post), :method => :delete %></span>

<% end %>
